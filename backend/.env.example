PORT=5000
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=YOUR_INFLUXDB_TOKEN_HERE
INFLUX_ORG=college
INFLUX_BUCKET=solarsoil
MQTT_BROKER=mqtt://broker.emqx.io:1883
MQTT_TOPIC=solarsoil/nodeA
NODE_ENV=development

# HTTPS / SSL (optional — for production with Cloudflare or other SSL)
# Generate Origin Certs at: Cloudflare Dashboard → SSL/TLS → Origin Server
# SSL_KEY_PATH=./certs/privkey.pem
# SSL_CERT_PATH=./certs/cert.pem
# SSL_CA_PATH=./certs/chain.pem
